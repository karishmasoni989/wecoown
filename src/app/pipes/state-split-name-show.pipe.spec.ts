import { StateSplitNameShowPipe } from './state-split-name-show.pipe';

describe('StateSplitNameShowPipe', () => {
  it('create an instance', () => {
    const pipe = new StateSplitNameShowPipe();
    expect(pipe).toBeTruthy();
  });
});
