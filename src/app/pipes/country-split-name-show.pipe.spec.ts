import { CountrySplitNameShowPipe } from './country-split-name-show.pipe';

describe('CountrySplitNameShowPipe', () => {
  it('create an instance', () => {
    const pipe = new CountrySplitNameShowPipe();
    expect(pipe).toBeTruthy();
  });
});
